name: Deploy Shiny App to shinyapps.io

on:
  push:
    branches:
      - main  # Triggers on push to the 'main' branch

jobs:
  deploy:
    runs-on: ubuntu-latest  # Use a clean Linux server for this job

    steps:
      # Step 1: Checkout code from GitHub
      - name: Checkout code
        uses: actions/checkout@v3

      # Step 2: Set up R
      - name: Set up R
        uses: r-lib/actions/setup-r@v2

      # Step 3: Install R package needed for deployment
      - name: Install rsconnect
        run: R -e 'install.packages("rsconnect", repos="https://cloud.r-project.org")'

      # Step 4: Deploy to shinyapps.io
      - name: Deploy to shinyapps.io
        env:
          SHINYAPPS_NAME: ${{ secrets.SHINYAPPS_NAME }}
          SHINYAPPS_TOKEN: ${{ secrets.SHINYAPPS_TOKEN }}
          SHINYAPPS_SECRET: ${{ secrets.SHINYAPPS_SECRET }}
        run: |
          R -e "rsconnect::setAccountInfo(name='${SHINYAPPS_NAME}', token='${SHINYAPPS_TOKEN}', secret='${SHINYAPPS_SECRET}')"
          R -e "rsconnect::deployApp(appDir='.', appName='my-shiny-app')"

